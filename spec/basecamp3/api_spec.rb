require "spec_helper"

describe Basecamp3::Api do
  it "has a version number" do
    expect(Basecamp3::Api::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
